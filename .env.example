# Example environment file for backend
# Copy this to .env and replace the placeholders with your values

# Server
PORT=5000
NODE_ENV=development
LOG_LEVEL=info

# Database - Option A: single connection string (recommended)
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://DB_USER:DB_PASS@DB_HOST:DB_PORT/DB_NAME

# Database - Option B: explicit variables (used if DATABASE_URL is not set)
# DB_CLIENT is optional; the code supports mysql. If you leave these empty
# the backend will fall back to SQLite at backend/data/database.sqlite
DB_CLIENT=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=hospital_system_002
DB_USER=root
DB_PASS=YourMySQLPasswordHere

# JWT secret used to sign tokens. Replace with a long, random string in prod.
JWT_SECRET=replace_with_a_long_random_string

# Optional: enable seeding or admin creation (project-specific)
# SEED_ADMIN=true

# Notes:
# - If you provide DATABASE_URL it will be used. Otherwise the DB_* vars
#   will be used. If neither is set the backend will use a local SQLite file.
# - Keep .env out of source control. Commit only this .env.example.